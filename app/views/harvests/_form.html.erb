<%= form_for(@harvest) do |f| %>
  <% if @harvest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@harvest.errors.count, "error") %> prohibited this harvest from being saved:</h2>

      <ul>
      <% @harvest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  
  <%= f.radio_button :animal_type, class: 'form-control' %><%= f.label :turkey %>
 
  <%= f.radio_button :animal_type, class: 'form-control' %> <%= f.label :deer %>
 
  <%= f.radio_button :animal_type, class: 'form-control' %> <%= f.label :moose %>
 
  <%= f.radio_button :animal_type, class: 'form-control' %> <%= f.label :bear %>
  <br>
  <br>
  
  <%= f.label :image %>
  <%= f.file_field :image %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
